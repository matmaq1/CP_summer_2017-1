/**
 * Created by Mateusz on 22.03.2017.
 */
import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;



public class TextFileRead {
    public static void main(String[] args) {

        /*
        System.out.print("write u fag ");
        Scanner scanner = new Scanner(System.in);
        String input = scanner.next();
        System.out.println(input);

divides the string into an array using the delimetere
String[] elem = currLine.split(",");

1 0 1
0 0 0
1 1 1 csv

tic tac toe
Read a file tell us if it's over write who is the winenr

        */
        //throws FileNotFoundException {
/*{
            File myfile = new File("C:\\file.txt");
            Scanner scanner = new Scanner(myfile);
            int i = 0;
            while (scanner.hasNext()) {
                String currLine = scanner.nextLine();
                System.out.println(i + " " + currLine);
                i++;
            }
        }
    } catch (FileNotFoundException)
    {
        System.out.println("kappa");
    }
/*
*/
    }
}
